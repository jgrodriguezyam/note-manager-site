@using NoteManager.Infrastructure.Enums

<ul class="nav navbar-top-links navbar-right pull-right">
    <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <i class="fa fa-user fa-fw"></i> <i class="fa fa-caret-down"></i>
        </a>
        <ul class="dropdown-menu dropdown-user">
            <li>
                <a href="@Url.Action(EAction.ChangePassword.ToString(), EController.User.ToString())"><i class="fa fa-lock fa-fw"></i>Cambiar Contraseña</a>
            </li>
            <li>
                <a href="@Url.Action(EAction.Index.ToString(), EController.Home.ToString())"><i class="fa fa-user fa-fw"></i>Perfil</a>
            </li>
            <li class="divider"></li>
            <li>
                <a href="@Url.Action(EAction.Logout.ToString(), EController.User.ToString())"><i class="fa fa-sign-out fa-fw"></i>Cerrar Sesion</a>
            </li>
        </ul>
    </li>
</ul>